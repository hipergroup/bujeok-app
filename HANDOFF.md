# 수호부 — 프로젝트 인수인계 문서

> 2026-08-04 기준. 다른 AI/기획자가 이 문서 하나로 전체 맥락을 파악할 수 있도록 정리.
> 스크린샷: `design/screenshots/` (6장). 라이브 앱: **https://hipergroup.github.io/bujeok-app/**

## 0. 한 줄 요약

**수호부** — "오늘의 마음을 지키는 부적". 만세력 기반 사주 + 짧은 상담 대화로 전통 부적 43종 중 맞춤 부적을 추천·생성하고, 꾸미고, 부적함에 모으고, 홈 화면 위젯으로 지니는 힐링 앱. 한지·붓글씨·낙관의 단일 디자인 언어(한지 디자인 시스템)로 전 화면 통일.

## 1. 현재 아키텍처 (구현 완료 상태)

- **웹앱**: Next.js 16 + Tailwind 4 + framer-motion, `output: "export"` 정적 빌드
- **배포**: main 푸시 → GitHub Actions → GitHub Pages 자동 배포 (`.github/workflows/deploy.yml`)
  - URL: https://hipergroup.github.io/bujeok-app/ (basePath `/bujeok-app`)
  - 서비스 워커(`public/sw.js`): 정적 자산 캐시 우선, 페이지는 네트워크 우선 → 배포 즉시 반영
- **iOS 네이티브**: WKWebView 얇은 껍데기(`ios/`) + WidgetKit 위젯. 웹만 푸시하면 앱 내용 자동 갱신
- **데이터**: 전부 localStorage. 백엔드·계정·서버 없음. AI 미연동(문구는 하드코딩 풀)
- **레포**: `hipergroup/bujeok-app` (main 브랜치). PRD·조사자료: `hipergroup/korean-talisman-guide`

## 2. 화면·탭 구조 (구현 완료)

**하단 탭 4개** (커스텀 SVG 아이콘, `src/components/BottomTab.tsx`):

| 탭 | 경로 | 내용 |
|---|---|---|
| 홈 | `/` | 인사말("{이름}님의 하루에 평안이 깃들기를") + 헤드라인 + 마음 카테고리 6칸 + 부적 만들기 CTA + 오늘의 운세 카드 |
| 부적 만들기 | `/talisman` | 4단계 생성 플로우 (아래 4번) |
| 내 부적함 | `/collection` | 저장 부적 2열 그리드(부적 SVG가 곧 카드), 기운 필터 칩, 탭하면 상세 모달(공유/다운로드/삭제) |
| 마이페이지 | `/mypage` | 프로필 카드(띠·삼재 배지·사주 4기둥·오행 차트), 통계 3칸(받은 부적/수집 n/43/연속 방문), 운세 흐름 7일 차트, 설정 |

- **부적 도감** `/encyclopedia`: 43종 카탈로그 + 검색 + 수집률. 하단 탭엔 없고 마이페이지에서 진입. ⚠️ 유일하게 구 다크 테마가 남은 화면(한지 리디자인 미적용)
- **온보딩**: 별도 라우트가 아니라 홈에 임베드(첫 실행 시). `/onboarding` 직접 접근도 가능(사주 정보 수정용)
- 홈 첫 로딩 스플래시: 한지 배경 + 앱 아이콘(프리렌더 HTML 포함)

## 3. 온보딩에서 정한 것

**4스텝 확정** (`src/app/onboarding/page.tsx`):

1. **웰컴** — 풀 워드마크 히어로 + 특징 3줄("만세력 기반 정확한 사주 풀이" / "마음을 담아 만드는 나만의 부적" / "오늘의 마음을 나누는 다정한 상담") + [시작하기]
2. **생년월일시 입력** — "태어난 날을 알려주세요". 년(1950~2010)·월·일 피커, **12지시 버튼**(자시~해시 + "모르겠어요"), 이름(선택, "인장에 사용됩니다"). 입력 즉시 입춘 기준 띠 미리보기 → [사주 풀이 보기]
3. **사주 풀이** — "당신은 {X}띠입니다", 사주팔자 4기둥(시·일·월·년주), 오행 균형 막대 5개 + 우세 오행 성격 설명, 삼재 경고 카드(들/눌/날삼재 + 삼재부 권장). 시간 미상이면 오시 기준 추정 표기 → [첫 부적 받기]
4. **첫 부적 선물** — "환영합니다! 첫 부적을 선물합니다": **호신부(護身符)** 실시간 SVG 생성(문구 "몸과 마음의 평안을 지켜드릴게요", 주문 "급급여율령(急急如律令)") → [부적함에 담기] → 홈

**저장**: `bujeok-user`(메인 프로필: 생년월일시·이름·띠·사주 간지·오행·삼재·첫부적) + `user_profile`(평면 폴백) + `onboarding_completed`. ⚠️ 선물 호신부는 부적함(`bujeok-collection`)에는 안 들어가 수집 카운트에 미반영(현 동작).

## 4. 부적 생성 방식

**phase 4단계**: 마음 선택(category) → 상담(chat) → 꾸미기(customize) → 완성(reveal). 진행점 표기는 "마음 나누기 → 부적 꾸미기 → 완성" 3단계.

### 4-1. 마음(기운) 선택 — `src/data/energies.ts` 7종
액운 막기(厄除)·마음 안정(平安)·소원 성취(所願)·건강 기원(健康)·가정 수호(家宅平安)·학업·시험(合格)·재복 기원(財福). 각 기운에 색·전통 문양(motif)·기본 색지(paper)·43종 카테고리 매핑이 정의됨. 홈은 앞 6종 2×3, 부적 만들기 화면은 7종 전부. 홈 카드 → `/talisman?energy={id}`로 상담 자동 시작.

### 4-2. 상담 대화 — `src/data/dialogues.ts`
카테고리별 4스텝 고정 흐름(보기 선택 + 모든 스텝 자유 입력 병행). 보기의 `keywordMap`으로 키워드 축적, 타이핑 인디케이터 1.2~1.5초. 결과: "당신에게 어울리는 부적을 찾았어요 ✨ 「{이름} ({한자})」".

### 4-3. 43종 추천 — `src/data/talismans.ts` (단일 원천, 2015줄)
수호 8·재물 7·건강 8·가정 7·학업 5·기타 8 = **43종**. 각 항목: id·name·hanja·description·meaning·situations·design(centerText/patterns/symbols/잉크·종이색)·usage·placement·replacement·mantra·colors (전통 조사 자료 기반). `getTalismanRecommendation(category, keywords)`: 키워드맵(100+) 부분일치 → 카테고리 필터 → 가중 점수(name/desc +3, situations +2, meaning +1)로 1종 확정.

### 4-4. 꾸미기 — 최근 "간소화" 방향 확정 (커밋 4937d6e)
**사용자가 고르는 건 딱 2가지**: ① 기원 문구(최대 24자, 직접 입력, 카테고리별 기본 문구 랜덤 제공) ② 화풍(전통/현대). 나머지는 **전부 자동**: 색지=기운의 기본 색지, 수호 동물=사용자 띠, 강조색=기운 색, 문양=추천 부적의 design, 인장=사용자 이름. (이전에 있던 색지 7종·12지 동물 선택 UI는 제거됨 — "선택 부담 줄이기"가 확정된 방향)
안내 카피: "지금, 마음을 한 줄로 적어보세요. / 당신의 마음이 부적이 됩니다."

### 4-5. 부적 렌더링 — `src/lib/talisman-generator.ts` (SVG 문자열 생성, 360×560)
- **전통 화풍**: 한지 텍스처 필터 + 이중 테두리 + 네 모서리 뇌문 + 두전(제목+한자) + 띠 동물 + **기원 문구 세로쓰기(오른쪽 열부터, 열당 7자)** + 주문 + 주홍 낙관(이름 2~3자 조판)
- **현대 화풍**: 라운드 + 그라데이션 + 점선 테두리 + 가로쓰기 + 이모지 장식
- **색지 7종**(`BACKGROUND_PRESETS`): 한지/홍지/백지/심야/남색/쑥색/황금 — 각각 잉크·글자색 세트 보유. 밝은 종이(한지·백지)일 때만 기운색이 테두리를 결정, 색지는 프리셋 잉크 우선(시안의 "남색지+금테" 재현)

### 4-6. 완성 화면
[부적함에 담기](localStorage `bujeok-collection`에 SVG 포함 저장 + iOS 위젯으로 PNG push) · 공유 3종([이미지 저장] 원본 3배 PNG / [스토리 카드] 1080×1920 / [정사각 카드] 1080×1080 — 다크+금박 카드 배경에 합성, 캡션 "✦ 수호부적 ✦") · [새 부적 만들기] · 하단 안전 고지 + 상담전화(109·1577-0199) tel 링크.

### 4-7. 위기 신호 감지 (자유 입력 전 단계에서 항상 실행)
`src/lib/crisis-detection.ts`: high(자살·자해 등 17개)/concern(우울·무기력 등 20개) 2단계 + 오탐 방지 문맥 규칙(관용 표현·연결어미 처리). **원문은 절대 로깅·저장·전송 안 함**(모듈 밖으로 안 나감). 감지 시 `CrisisSupport` 모달(부드러운 톤, 빨강 금지): high "잠시 멈춰서, 이야기를 나눠요" / concern "괜찮으신가요?" + 상담전화(109·1577-0199·1388). 어떤 버튼으로 닫아도 답변 유실 없이 대화 계속. 이후 위로 모드로 전환되어 **"정신안정부"**로 안내(메시지: "오늘도 잘 버텼어요, 수고했어요" 등 4종). ⚠️ 정신안정부는 43종 카탈로그에 실물이 없어 치병부 메타로 폴백되는 알려진 불일치 있음.

## 5. 위젯 활용 (구현 완료 — iOS)

**컨셉: "부적은 시간이 지나면 낡는 물건"** — 디지털로 물성을 재현.

- WidgetKit 홈 화면 위젯 3종(소/중/대), kind `BujeokTalisman`, 표시명 "수호부", 설명 "당신의 하루를 지켜주는 작은 부적"
- **보여주는 것**: 사용자가 마지막으로 부적함에 담은 부적 이미지 1장 (풀블리드)
- **낡아감 연출**: 저장일로부터 45일에 걸쳐 갈변(multiply 블렌드) + 비네트가 점점 짙어짐. 매일 자정 타임라인 갱신(7일치 엔트리 + atEnd)
- **빈 상태**: 주홍 배지에 "수호/부" + "부적을 만들어 보세요"
- **데이터 흐름(단방향 1경로)**: 웹 `handleSave`(부적함에 담기) → `src/lib/widget-bridge.ts` `pushTalismanToWidget` → WKWebView `widgetBridge` postMessage(PNG base64 + 이름/한자/날짜) → App Group(`group.com.juno.bujeok`) 컨테이너에 `widget-talisman.png` + `widget-meta.json` 파일 저장 → `reloadAllTimelines()`. 브라우저/웹클립에서는 자동 no-op
- iOS 프로젝트는 xcodegen 선언형(`ios/project.yml`이 소스 오브 트루스, pbxproj 직접 수정 금지). 번들 `com.juno.bujeok`(+`.widget`), 팀 G6592L5687, 배포 타깃 iOS 17
- **아직 없음(다음 확장 지점)**: 위젯 탭 딥링크(widgetURL), note 문구 표시, 부적 여러 개 로테이션, 잠금화면 위젯

## 6. 확정된 이름·카피

- **앱 이름**: 수호부 (⚠️ 공유 카드 캡션 "✦ 수호부적 ✦"·안전 고지 본문은 "수호부적" — 표기 혼재, 통일 필요)
- **태그라인**: "오늘의 마음을 지키는 부적" (워드마크 록업에 포함)
- **메타**: title "수호부 - 마음을 지키는 부적" / description "전통 부적을 현대적으로 재해석한 힐링 앱. 마음을 한 줄로 적으면 당신만의 부적이 됩니다."
- **홈 헤드라인**: "오늘, / 어떤 마음을 / 지키고 싶으신가요?" (붓글씨체) — 위 "{이름}님의 하루에 평안이 깃들기를", 아래 "마음의 방향을 선택해 보세요."
- **홈 CTA**: "나만의 부적 만들기 / 마음을 담아 부적을 직접 만들어 보세요."
- **부적 만들기**: "어떤 마음을 담아볼까요? / 마음의 방향을 고르면 짧은 대화 후 부적을 만들어 드려요." · 채팅 헤더 "{기운} · 마음 나누기"
- **주요 버튼**: 시작하기 · 사주 풀이 보기 · 첫 부적 받기 · 부적함에 담기 → ✓ 부적함에 담았습니다 · 부적 만들기 · 부적 완성하기 · 이미지 저장/스토리 카드/정사각 카드 · 새 부적 만들기 · 부적 만들러 가기
- **부적함**: "내 부적함 / 당신이 만든 부적들을 보관해 보세요." (빈 상태: "아직 부적이 없어요 / 마음을 담아 첫 부적을 만들어 볼까요?")
- **도감**: "부적 도감 / 전통 부적 43종 / 43종 중 n종 수집"
- **온보딩 특징 3줄**: "만세력 기반 정확한 사주 풀이 / 마음을 담아 만드는 나만의 부적 / 오늘의 마음을 나누는 다정한 상담"
- **위젯**: "수호부 — 당신의 하루를 지켜주는 작은 부적" (빈 상태: "부적을 만들어 보세요")
- **안전 고지**: "수호부적은 정서적 위로와 응원을 위한 서비스입니다. / 의료·법률·금융 조언을 제공하지 않으며, / 전문적인 도움이 필요할 때는 전문가와 상담해 주세요."
- **공유 문구**: "✨ 나만의 부적 「{이름}」을 만들었어요 🙏" · 파일명 `수호부_{부적명}.png`
- **버전**: 마이페이지 하단 "수호부 v1.1"

## 7. 디자인 시스템 (한지 디자인 언어)

### 팔레트 (`src/app/globals.css`, Tailwind v4 `@theme`)

| 토큰 | Hex | 용도 |
|---|---|---|
| `--color-juhong` | `#A72B21` | 주홍·인주 (CTA·낙관·활성 탭·테두리) |
| `--color-hanji` | `#F2E6CC` | 한지 배경 (themeColor·iOS 배경과 동일) |
| `--color-meok` | `#2E2E2E` | 먹 (본문) |
| `--color-galsaek` | `#7A4A34` | 짙은 갈색 (보조 텍스트·선) |
| `--color-hwang` | `#DAA017` | 겨자·황 |
| `--color-namsaek` | `#1F3E63` | 짙은 남 |
| `--color-ssuk` | `#6B7D63` | 쑥·세이지 |
| `--color-beige` | `#DCC9A5` | 보조 베이지 |
| (리터럴) | `#F6EDD9` | hanji-card 종이색 (토큰화 안 됨) |

### 폰트
- **Gowun Batang** (400/700) = `--font-serif-kr` — 본문·제목 명조
- **Song Myung** (400) = `--font-brush` — 큰 제목·낙관 붓글씨풍
- **Pretendard** (CDN) — body 기본 산세리프

### 공용 클래스·컴포넌트
- `.hanji-surface`(화면 바탕: 한지색+갈색 바램+SVG 노이즈) / `.hanji-card`(종이 카드: #F6EDD9+노이즈+갈색 테두리+이중 그림자)
- `src/components/hanji/`: `HanjiBackground`(+decorated 구름·불꽃 장식) · `TraditionalHeader`(워드마크/낙관/제목 중앙) · `TraditionalButton`(주홍+베이지 이중 테두리 CTA) · `TalismanCategoryCard` · `EnergySelector`(미사용) · `motifs.tsx`(전통 SVG 세트)
- **motifs 아이콘**: 기운 문양 6종(매듭·구름·불꽃·산수·장승·연꽃), SealLogo(낙관), BrushStroke(갈필), BrushPen(붓), CornerMotif(뇌문), 헤더 아이콘(메뉴·풍경 종·뒤로·꽃살문 기어), 탭 아이콘 4종(기와집·붓·함·갓 쓴 선비) — 전부 `currentColor` 선화
- **원칙**(AGENTS.md 명문화): 부적 데이터 단일 원천 = talismans.ts, UI는 한지 시스템 재사용

### ⚠️ 톤 이원화 잔재 (다음 디자인 작업의 핵심)
- globals.css에 구 다크 미스티컬 토큰(`--color-gold #D4A853`, bg `#0a0a1a` 등) 공존, body 배경도 아직 다크(화면은 HanjiBackground가 덮음)
- **한지 톤 미적용 화면·요소**: `/encyclopedia` 전체(다크), `TalismanThumbnail`(다크·금색), 공유 카드 배경(다크+금박 — 의도적일 수도 있으나 결정 필요)

## 8. 브랜드 에셋

| 파일 | 내용 | 용도 |
|---|---|---|
| `design/app-icon-source.png` | 앱 아이콘 원본 1254px (한지에 수호부 서예+노리개·방울·守護符 태그) | 원본 보관 |
| `src/app/icon.png` / `favicon.ico` / `apple-icon.png` | 512/멀티/180px | 파비콘·홈화면 아이콘 |
| `ios/Bujeok/Assets.xcassets/AppIcon…/AppIcon1024.png` | 1024px 한지색 배경 | iOS 앱 아이콘 |
| `design/wordmark-source.png` | 워드마크 스펙 시트 원본 1774px | 원본 보관 |
| `public/brand/wordmark.png` | 풀 록업 1000px 투명배경 (수호부 붓글씨 + 守護符印 낙관 + 태그라인) | 온보딩 히어로 |
| `public/brand/wordmark-mark.png` | 축약판 480px (태그라인 제외) | 홈 헤더 |
| `public/brand/app-icon.png` | 256px 경량판 | 로딩 스플래시 |

## 9. PRD 요구사항 대비 진행 상황

PRD 원문: `hipergroup/korean-talisman-guide`의 `05_앱_PRD_보강안.md` (01~04 문서 = 부적 역사·43종 의미·문양 상징·실용 가이드, images/ = 부적 SVG 12종)

**PRD의 탭 구조 보강안**: [홈(운세·체크인·행운요소·한줄주문·시즌)] [부적만들기(카테고리→상담→커스텀→완성)] [부적함(받은부적·수집률·일기·교체알림)] [도감(43종·문양해설·상징사전·검색)] [마이(사주·운세히스토리·알림·개인정보·지원)]

**Phase 로드맵과 현재 위치**:
- Phase 1 (MVP): 온보딩·사주·운세·상담·부적 생성 — ✅ 구현됨 (43종 매핑, 화풍 2종, 사주 기반 운세)
- Phase 2 (수집/커스텀): 도감 ✅ · 부적함 ✅ · 커스터마이징 ✅(최근 "화풍만 선택, 나머지 자동"으로 간소화) · 공유 카드 ✅ · 부적 선물하기 ❌
- Phase 3 (이벤트/소셜): 시즌 이벤트 ❌ · 감정 트래킹 ❌ · 부적 일기 ❌ · 인장 커스텀 ❌
- PRD에 없던 추가 구현: **위기 신호 감지·전문기관 안내**, **홈 화면 위젯**, **iOS 네이티브 래퍼**, 서비스 워커

## 10. 코드 구현 현황 요약

### 구현 완료 ✅
온보딩 4스텝(만세력 사주: 24절기 VSOP87 천문 계산, 1900~2100 오차 1분 이내 · 입춘 기준 띠 · 삼재) / 홈(사주 기반 오늘의 운세 — 같은 사람·같은 날 동일 결과, 행운색=부족 오행) / 부적 생성 전체 플로우(7기운→상담→43종 추천→꾸미기→완성) / 위기 감지·전문기관 안내 / 부적함(필터·상세·삭제) / 도감(43종·검색·수집률) / 마이페이지(사주 카드·통계·운세 7일 차트·데이터 초기화) / 공유 카드 3종 / iOS 래퍼+홈 위젯(45일 낡아감) / 서비스 워커 / 웹클립 배포

### 데이터 (백엔드 없음 — 전부 localStorage)
`onboarding_completed` · `bujeok-user`(프로필+사주) · `user_profile`(폴백) · `bujeok-collection`(저장 부적, SVG 포함) · `bujeok-visit-log`(연속 방문). ⚠️ `src/lib/store.ts`(`bujeok_app_v1` 통합 스토어)는 완성돼 있으나 실제 화면과 연결 안 된 죽은 코드.

### 미구현 (UI상 "준비 중" alert)
홈 메뉴 · 알림 · 각 화면 설정 · 알림 설정. Phase 2 잔여: 부적 선물하기. Phase 3 전체: 시즌 이벤트·감정 트래킹·부적 일기·인장 커스텀. 운세 세부(재물/애정/건강/학업운·행운 방위·숫자·일일 주문)는 **계산은 되는데 화면에 미노출**.

### 알려진 리스크·정리 후보 (인수 후 우선 확인)
1. **정신안정부 카탈로그 부재** — 위기 모드 부적이 치병부 메타로 폴백 (`talisman/page.tsx`)
2. **저장소 이중화** — 실사용 키 vs 미사용 store.ts, 동명 타입 `SavedTalisman` 2개
3. **`/encyclopedia`만 구 다크 테마** + 하단 탭 진입 불가
4. **데드코드 ~1800줄** — `TalismanSVG.tsx`(1073) · `TalismanReveal.tsx`(422) · `download-talisman.ts`(303) · `EnergySelector.tsx` 미사용
5. **표기 혼재** — 수호부 vs 수호부적
6. 온보딩 선물 호신부가 부적함 카운트에 미반영

### 다음 방향 (이미 합의된 흐름)
기획을 처음부터 다시 하지 않고, **수호부 비주얼(한지 디자인 언어)을 기존 UX 전체에 입히는 것**: 인트로 → 온보딩 → 메인 → 부적 만들기 → 부적함 → 위젯을 하나의 디자인 언어로. 남은 대상: 도감 한지화, 썸네일·공유 카드 톤 결정, 다크 토큰 정리.

## 11. 협업 규칙 (중요)

- 여러 Claude 세션이 병행 작업 → **push 전 반드시 `git pull` 후 병합, force push 금지** (사고 이력 2회)
- **gh-pages 브랜치 직접 푸시 금지** — Actions가 자동 관리. 복구: `gh workflow run deploy.yml --ref main`
- GitHub Pages basePath 때문에 생 `<a href="/...">`·`window.location.href='/'` 금지, next/link·정적 import만 사용
- 수정 중이어도 자주 커밋·푸시해 팀원이 GitHub에서 보게 할 것

## 12. 스크린샷 (design/screenshots/)

| 파일 | 화면 |
|---|---|
| `01-onboarding-welcome.png` | 온보딩 웰컴 — 풀 워드마크 + 3개 특징 카드 + 시작하기 |
| `02-home.png` | 홈 — 워드마크 헤더, "오늘, 어떤 마음을 지키고 싶으신가요?", 기운 카드 2열, 부적 만들기 CTA, 4탭 |
| `03-talisman-flow.png` | 부적 만들기 1단계 — 마음(기운) 7종 선택, 3단계 진행점 |
| `04-collection.png` | 내 부적함 |
| `05-encyclopedia.png` | 부적 도감 |
| `06-mypage.png` | 마이페이지 |
